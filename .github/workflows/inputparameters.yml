---
name: inputparameters
on:
  workflow_dispatch:
    inputs:
      environment:
        description: this is inputparameters code
        required: true
        default: test
        type: choice
        options:
          - dev
          - uat
          - prod
          - sri
      owner:
        description: this owner is sri
        required: true
        default: 93926
jobs:
  inputparameter:
    runs-on: ubuntu-latest
    steps:
      - name: run input parameter
        run: echo "Deploy the ${{inputs.owner}} application ${{inputs.environment}}"
